# NNNN. <Decision title, stated as a decision — "Use X for Y">

Date: YYYY-MM-DD
Status: accepted | superseded by [NNNN](NNNN-slug.md)

<!--
Lives at `docs/adr/NNNN-slug.md`. NNNN is the next unused four-digit number —
`ls docs/adr/` and add one; numbers are never reused, even for a deleted draft.

Long-lived decisions only: the ones a future discovery pass must know before
proposing something that conflicts with them. A choice that only matters inside
one piece of work belongs in that work's plan, not here.

Written once, amended never — a decision that changes gets a new ADR and the
old one's Status becomes `superseded by`. Keep it under a page.
-->

## Context

<!-- The forces at play: the problem, the constraints, what made this decision
necessary. Written so it still makes sense in two years, to someone who was not
in the conversation. -->

## Decision

<!-- What we decided, in active voice. "We will …" -->

## Alternatives considered

<!-- Each rejected option with one honest line on why it lost. This is the
section that stops the same debate being re-litigated by the next agent — an
ADR without it is a decision with no immune system. -->

- **<Option>** — rejected because <reason>.

## Consequences

<!-- What becomes easier, what becomes harder, what we are now committed to.
Include the negative consequences — an ADR with no downsides is marketing. -->
