# mealmate (eval fixture)

Fixture world for the brainstorm `resume-existing-space` eval: a meal-planning PWA project
whose `docs/better-planning/` space is **mid-flight** — settled brief and product PRD,
pantry-sync feature PRD in-review with exactly one open question (P7, barcode scope), and
shopping-list-gen next per the roadmap. The eval checks that a resuming skill digests this
state, re-asks nothing settled, and modifies no fixture docs.

This parent README is not part of the fixture — `prepare` copies `mid-flight/` only, so the
world stays free of eval-harness markers. Not a real project.
